# Requested change

`summarize` currently reports `count` and `totalCents`. Add an `averageCents` field: the integer average of the record amounts, rounded to the nearest cent, and `0` for empty input. Cover the new field in the unit tests.
