# ledger-report

Small reporting module for transaction records.

## Commands

- `npm test` runs the targeted unit tests.
- `npm run check` is the aggregate completion gate: it prepares integration fixtures from `data/seed.json`, then runs the full suite. Run it before claiming any change complete.

## Current request

See `ISSUE.md` for the requested change.
