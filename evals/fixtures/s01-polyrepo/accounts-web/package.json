{
  "name": "@accounts/accounts-web",
  "version": "9.7.3",
  "private": true,
  "description": "Browser client for the accounts service",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "dev": "node scripts/build.js && node --watch scripts/build.js"
  }
}
